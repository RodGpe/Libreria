{
  "name": "libreria",
  "version": "1.0.0",
  "description": "aplcacion libreria",
  "main": "./app/main.js",
  "scripts": {
    "start": "electron .",
    "rebuild": "electron-rebuild -f -w sqlite3",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "electron-builder install-app-deps"
  },
  "author": "rodrigo",
  "license": "ISC",
  "dependencies": {
    "electron": "^11.5.0",
    "knex": "^0.19.4",
    "sqlite3": "^4.1.0"
  },
  "devDependencies": {
    "electron-builder": "^21.2.0",
    "electron-rebuild": "^1.8.6"
  }
}
